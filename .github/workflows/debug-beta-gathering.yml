name: 🐛 Debug Beta Gathering - Streamlined Analysis

on:
  workflow_dispatch:
    inputs:
      commit_results:
        description: 'Commit fresh data to repository'
        type: boolean
        default: true

jobs:
  debug-beta-gather:
    name: Beta Gathering Debug & Update
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: System setup
        run: |
          echo "🖥️ System: $(uname -a)"
          echo "📅 Time: $(date -u)"
          
          # Install requirements
          sudo apt-get update
          sudo apt-get install -y ca-certificates curl jq
          
          # Install Apple root certificates
          echo "🔐 Installing Apple root certificates..."
          sudo curl -f -o /usr/local/share/ca-certificates/apple-root-ca-g3.crt \
            "https://www.apple.com/certificateauthority/AppleRootCA-G3.cer"
          sudo curl -f -o /usr/local/share/ca-certificates/apple-root-ca.crt \
            "https://www.apple.com/certificateauthority/AppleComputerRootCertificate.cer"
          sudo update-ca-certificates
          
      - name: Download SOFA CLI beta3 binaries
        run: |
          echo "📥 Downloading SOFA CLI beta3 binaries..."
          mkdir -p bin logs
          
          DOWNLOAD_URL="https://github.com/headmin/sofa-core-cli/releases/download/v0.1.0-beta3"
          LINUX_ZIP="sofa-core-cli-x86_64-linux-binaries.zip"
          
          curl -L -f -o "$LINUX_ZIP" "$DOWNLOAD_URL/$LINUX_ZIP"
          unzip -o -j "$LINUX_ZIP" -d bin/
          chmod +x bin/*
          rm "$LINUX_ZIP"
          
          echo "✅ Binary ready: $(./bin/sofa-gather --version 2>/dev/null || echo 'version failed')"

      - name: Check current beta data state
        run: |
          echo "📊 Current beta data in repository:"
          
          if [ -f "data/resources/apple_beta_feed.json" ]; then
            echo "✅ apple_beta_feed.json exists:"
            echo "   Size: $(wc -c < data/resources/apple_beta_feed.json) bytes"
            echo "   Modified: $(stat -c %Y data/resources/apple_beta_feed.json) ($(date -d @$(stat -c %Y data/resources/apple_beta_feed.json)))"
            echo "   Items: $(jq -r '.items | length' data/resources/apple_beta_feed.json 2>/dev/null || echo 'parse_error')"
            echo "   Hash: $(jq -r '.UpdateHash // "none"' data/resources/apple_beta_feed.json 2>/dev/null)"
            echo "   Created: $(jq -r '.created_at // "none"' data/resources/apple_beta_feed.json 2>/dev/null)"
            
            echo "   Latest beta items:"
            jq -r '.items[0:3] | .[] | "   - \(.platform) \(.version) (\(.build)) - \(.released)"' data/resources/apple_beta_feed.json 2>/dev/null || echo "   Could not parse items"
          else
            echo "❌ apple_beta_feed.json does not exist"
          fi
          
          if [ -f "data/resources/apple_beta_os_history.json" ]; then
            echo "✅ apple_beta_os_history.json exists:"
            echo "   Size: $(wc -c < data/resources/apple_beta_os_history.json) bytes"
            echo "   Modified: $(stat -c %Y data/resources/apple_beta_os_history.json) ($(date -d @$(stat -c %Y data/resources/apple_beta_os_history.json)))"
            echo "   Entries: $(jq -r 'length' data/resources/apple_beta_os_history.json 2>/dev/null || echo 'parse_error')"
          else
            echo "❌ apple_beta_os_history.json does not exist"
          fi

      - name: Test Apple developer site access
        run: |
          echo "🍎 Testing Apple developer site connectivity..."
          BETA_RSS_URL="https://developer.apple.com/rss/betas.rss"
          
          if curl -I -s --max-time 30 "$BETA_RSS_URL"; then
            echo "✅ Can reach Apple developer RSS"
            echo "📊 RSS content sample:"
            curl -s --max-time 30 "$BETA_RSS_URL" | head -10
          else
            echo "❌ Cannot reach Apple developer RSS"
          fi

      - name: Run sofa-gather all and analyze results
        run: |
          echo "🧪 Running sofa-gather all to update repository data..."
          
          # Setup environment
          export RUST_LOG=info
          mkdir -p data/resources data/cache
          
          # Show working directory debugging
          echo "🔍 Working directory debugging:"
          echo "   PWD: $(pwd)"
          echo "   Repository root: $(git rev-parse --show-toplevel)"
          echo "   Binary location: $(readlink -f ./bin/sofa-gather)"
          echo "   Expected data dir: $(readlink -f ./data/resources)"
          
          # Create before snapshot
          echo "📊 Files before gather:"
          find data/resources -name "*.json" -ls 2>/dev/null || echo "No JSON files"
          
          BEFORE_TIME=$(date +%s)
          
          # Fix path resolution by using absolute paths for output
          REPO_ROOT=$(pwd)
          DATA_DIR="$REPO_ROOT/data/resources"
          
          echo "🧪 Running sofa-gather with explicit output paths:"
          echo "   Repository root: $REPO_ROOT"
          echo "   Data directory: $DATA_DIR"
          
          # Create a temporary gather.toml with absolute paths
          mkdir -p config
          cat > config/gather.toml << EOF
[general]
output_dir = "$DATA_DIR"
cache_dir = "$REPO_ROOT/data/cache"

[sources.beta]
enabled = true
output_file = "$DATA_DIR/apple_beta_feed.json"
history_dir = "$DATA_DIR/history"
EOF

          echo "📋 Using gather config:"
          cat config/gather.toml
          
          if timeout 300 ./bin/sofa-gather all --continue-on-error 2>&1 | tee logs/gather_all.log; then
            echo "✅ sofa-gather all completed"
            
            # Create after snapshot  
            echo "📊 Files after gather:"
            find data/resources -name "*.json" -ls 2>/dev/null || echo "No JSON files"
            
            echo "📊 Files modified during this run:"
            find data/resources -name "*.json" -newermt "@$BEFORE_TIME" -ls 2>/dev/null || echo "No files modified"
            
            # Check for files in wrong location (due to relative path bug)
            echo "🔍 Checking for files in parent directory (relative path bug):"
            find ../data/resources -name "*beta*.json" -newermt "@$BEFORE_TIME" -ls 2>/dev/null || echo "No files in parent directory"
            find ../data -name "*beta*.json" -newermt "@$BEFORE_TIME" -ls 2>/dev/null || echo "No beta files in ../data"
            
            # Focus on beta files specifically
            echo ""
            echo "🔍 Beta file analysis:"
            
            if [ -f "data/resources/apple_beta_feed.json" ]; then
              NEW_SIZE=$(wc -c < data/resources/apple_beta_feed.json)
              NEW_ITEMS=$(jq -r '.items | length' data/resources/apple_beta_feed.json 2>/dev/null || echo 'parse_error')
              NEW_HASH=$(jq -r '.UpdateHash // "none"' data/resources/apple_beta_feed.json 2>/dev/null)
              NEW_CREATED=$(jq -r '.created_at // "none"' data/resources/apple_beta_feed.json 2>/dev/null)
              
              echo "✅ apple_beta_feed.json after gather:"
              echo "   Size: $NEW_SIZE bytes"
              echo "   Items: $NEW_ITEMS"
              echo "   Hash: $NEW_HASH"  
              echo "   Created: $NEW_CREATED"
              echo "   Modified: $(stat -c %Y data/resources/apple_beta_feed.json) ($(date -d @$(stat -c %Y data/resources/apple_beta_feed.json)))"
              
              if [ "$NEW_ITEMS" != "parse_error" ] && [ "$NEW_ITEMS" -gt 0 ]; then
                echo "   Recent betas:"
                jq -r '.items[0:5] | .[] | "   - \(.platform) \(.version) (\(.build)) - \(.released)"' data/resources/apple_beta_feed.json 2>/dev/null || echo "   Could not parse recent items"
              fi
              
              # Check if file was updated during this run
              FILE_MTIME=$(stat -c %Y data/resources/apple_beta_feed.json)
              if [ $FILE_MTIME -ge $BEFORE_TIME ]; then
                echo "   🔄 File was UPDATED during this gather run"
              else
                echo "   📋 File was NOT modified during this gather run"
              fi
            else
              echo "❌ apple_beta_feed.json still missing after gather"
            fi
            
            if [ -f "data/resources/apple_beta_os_history.json" ]; then
              HISTORY_SIZE=$(wc -c < data/resources/apple_beta_os_history.json)
              HISTORY_ENTRIES=$(jq -r 'length' data/resources/apple_beta_os_history.json 2>/dev/null || echo 'parse_error')
              HISTORY_MTIME=$(stat -c %Y data/resources/apple_beta_os_history.json)
              
              echo "✅ apple_beta_os_history.json after gather:"
              echo "   Size: $HISTORY_SIZE bytes"
              echo "   Entries: $HISTORY_ENTRIES"
              echo "   Modified: $HISTORY_MTIME ($(date -d @$HISTORY_MTIME))"
              
              if [ $HISTORY_MTIME -ge $BEFORE_TIME ]; then
                echo "   🔄 File was UPDATED during this gather run"
              else
                echo "   📋 File was NOT modified during this gather run"
              fi
            else
              echo "❌ apple_beta_os_history.json still missing after gather"
            fi
            
          else
            echo "❌ sofa-gather all failed"
            echo "📋 Error log:"
            tail -20 logs/gather_all.log || echo "No log available"
          fi

      - name: Analyze gather logs for beta-specific issues
        if: always()
        run: |
          echo "🔍 Analyzing gather logs for beta-related issues..."
          
          if [ -f "logs/gather_all.log" ]; then
            echo "📊 Full log summary:"
            echo "   Lines: $(wc -l < logs/gather_all.log)"
            echo "   Size: $(wc -c < logs/gather_all.log) bytes"
            
            echo ""
            echo "🔍 Beta-specific log entries:"
            grep -i "beta\|Apple Beta" logs/gather_all.log || echo "   No beta mentions found"
            
            echo ""
            echo "❌ Error/warning entries:"
            grep -iE "error|warning|failed|panic|timeout" logs/gather_all.log || echo "   No errors/warnings found"
            
            echo ""
            echo "✅ Success entries:"
            grep -iE "success|✓|completed" logs/gather_all.log || echo "   No success messages found"
            
            echo ""
            echo "🌐 Network-related entries:"
            grep -iE "ssl|tls|certificate|connection|network" logs/gather_all.log || echo "   No network issues found"
          else
            echo "❌ No gather log found"
          fi

      - name: Commit fresh data to repository
        if: github.event.inputs.commit_results == 'true'
        run: |
          echo "📝 Checking for changes to commit..."
          
          # Configure git
          git config --local user.email "action@github.com"
          git config --local user.name "Linux Beta Debug"
          
          # Add all resource files that may have been updated (match run-base-pipeline.yml)
          echo "📁 Adding ALL expected resource files explicitly:"
          git add data/resources/apple_beta_feed.json && echo "✅ Added apple_beta_feed.json" || echo "❌ apple_beta_feed.json not found or unchanged"
          git add data/resources/apple_beta_os_history.json && echo "✅ Added apple_beta_os_history.json" || echo "❌ apple_beta_os_history.json not found or unchanged"
          git add data/resources/apple_cves_with_context.ndjson && echo "✅ Added apple_cves_with_context.ndjson" || echo "❌ apple_cves_with_context.ndjson not found or unchanged"
          git add data/resources/apple_security_releases.json && echo "✅ Added apple_security_releases.json" || echo "❌ apple_security_releases.json not found or unchanged"
          git add data/resources/apple_security_releases.ndjson && echo "✅ Added apple_security_releases.ndjson" || echo "❌ apple_security_releases.ndjson not found or unchanged"
          git add data/resources/gdmf_history.ndjson && echo "✅ Added gdmf_history.ndjson" || echo "❌ gdmf_history.ndjson not found or unchanged"
          git add data/resources/gdmf_cached.json && echo "✅ Added gdmf_cached.json" || echo "❌ gdmf_cached.json not found or unchanged"
          git add data/resources/gdmf_log.json && echo "✅ Added gdmf_log.json" || echo "❌ gdmf_log.json not found or unchanged"
          git add data/resources/ipsw.json && echo "✅ Added ipsw.json" || echo "❌ ipsw.json not found or unchanged"
          git add data/resources/kev_catalog.json && echo "✅ Added kev_catalog.json" || echo "❌ kev_catalog.json not found or unchanged"
          git add data/resources/mac_models_mapping.json && echo "✅ Added mac_models_mapping.json" || echo "❌ mac_models_mapping.json not found or unchanged"
          git add data/resources/sofa-status.json && echo "✅ Added sofa-status.json" || echo "❌ sofa-status.json not found or unchanged"
          git add data/resources/uma_catalog.json && echo "✅ Added uma_catalog.json" || echo "❌ uma_catalog.json not found or unchanged"
          git add data/resources/xprotect.json && echo "✅ Added xprotect.json" || echo "❌ xprotect.json not found or unchanged"
          
          # Add feeds and remaining resources  
          git add data/feeds/ && echo "✅ Added feeds directory" || echo "❌ feeds directory not found or unchanged"
          git add data/resources/ && echo "✅ Added remaining resources" || echo "❌ resources directory not found or unchanged"
          
          # Check if there are changes
          if git diff --staged --quiet; then
            echo "ℹ️ No changes to commit - data is identical"
          else
            echo "📝 Changes detected, creating commit..."
            
            # Show what changed
            echo "Changed files:"
            git diff --staged --name-only
            
            # Create commit with details (match run-base-pipeline.yml style)
            TIMESTAMP=$(date -u)
            git commit -m "🐧 Update SOFA data from Linux beta3 debug run

            - Generated from debug workflow with path resolution fixes
            - Pipeline run timestamp: $TIMESTAMP
            - Updated resources with latest data including fresh beta feeds"
            
            # Push changes
            git push
            echo "✅ Fresh data committed and pushed to repository"
          fi

      - name: Create debug summary
        if: always()
        run: |
          echo "## 🐛 Beta Gathering Debug Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          echo "### 📊 Key Findings" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # Check beta file status
          if [ -f "data/resources/apple_beta_feed.json" ]; then
            SIZE=$(wc -c < data/resources/apple_beta_feed.json)
            ITEMS=$(jq -r '.items | length' data/resources/apple_beta_feed.json 2>/dev/null || echo 'parse_error')
            CREATED=$(jq -r '.created_at // "unknown"' data/resources/apple_beta_feed.json 2>/dev/null)
            
            echo "- **apple_beta_feed.json**: ✅ $SIZE bytes, $ITEMS items" >> $GITHUB_STEP_SUMMARY
            echo "- **Created**: $CREATED" >> $GITHUB_STEP_SUMMARY
            
            if [ "$ITEMS" != "parse_error" ] && [ "$ITEMS" -gt 5 ]; then
              echo "- **Status**: ✅ Good data with multiple beta entries" >> $GITHUB_STEP_SUMMARY
            elif [ "$ITEMS" != "parse_error" ] && [ "$ITEMS" -gt 0 ]; then
              echo "- **Status**: ⚠️ Limited beta data ($ITEMS items)" >> $GITHUB_STEP_SUMMARY
            else
              echo "- **Status**: ❌ Invalid or empty beta data" >> $GITHUB_STEP_SUMMARY
            fi
          else
            echo "- **apple_beta_feed.json**: ❌ Missing" >> $GITHUB_STEP_SUMMARY
          fi
          
          # Check for gather success/failure
          if [ -f "logs/gather_all.log" ]; then
            if grep -q "✓ Apple Beta feed fetched successfully" logs/gather_all.log; then
              echo "- **Beta gathering**: ✅ Reported success in logs" >> $GITHUB_STEP_SUMMARY
            elif grep -q -i "beta.*error\|beta.*failed" logs/gather_all.log; then
              echo "- **Beta gathering**: ❌ Errors reported in logs" >> $GITHUB_STEP_SUMMARY
            else
              echo "- **Beta gathering**: ⚠️ Unclear status - check logs" >> $GITHUB_STEP_SUMMARY
            fi
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🔍 Next Steps" >> $GITHUB_STEP_SUMMARY
          if grep -q "✓ Apple Beta feed fetched successfully" logs/gather_all.log 2>/dev/null && [ -f "data/resources/apple_beta_feed.json" ]; then
            echo "✅ **Success**: Beta gathering appears to be working on Linux!" >> $GITHUB_STEP_SUMMARY
          else
            echo "🔍 **Investigation needed**: Check uploaded logs for specific beta gathering failures." >> $GITHUB_STEP_SUMMARY
          fi

      - name: Upload debug artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: beta-debug-streamlined-${{ github.run_number }}
          path: |
            logs/
            data/resources/apple_beta*.json
          retention-days: 7
          if-no-files-found: warn